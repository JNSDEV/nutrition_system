---
phase: 04.1-library-wiring-fixes
plan: "01"
subsystem: library
tags: [session-type-mapping, macro-templates, farva-rename, blocker-02]
dependency_graph:
  requires: []
  provides: [session_type_mapping, archetype_tokens, farva_label_in_macro_templates]
  affects: [CAL-02, CMD-01, CMD-02, CMD-03, CMD-05, CMD-06]
tech_stack:
  added: []
  patterns: [session_type_to_archetype_lookup_table]
key_files:
  modified:
    - library/macro-templates.md
decisions:
  - "Used 12-row mapping table covering every distinct cycling-2026.md Session label including event markers (SPORTIVE, BENCHMARK, REHEARSAL, HEATHLAND)"
  - "Strength (full body) mapped to lower_carb / Rest Day Template per D-08 context (not training load)"
  - "Fasted Z2 row mapped as medium_ride / moderate_carb (same as Z2 Zwift — comparable duration/intensity)"
  - "H3 headings under ## Jonas / ## Farva sections (not H2) to keep section hierarchy clean"
  - "archetype: tag added to each Jonas template heading only; Farva templates are static so no archetype tag needed"
metrics:
  duration_seconds: 48
  completed: 2026-05-07T19:44:11Z
  tasks_completed: 1
  files_modified: 1
---

# Phase 4.1 Plan 01: Session-Type Mapping Summary

Session-Type Mapping table added to macro-templates.md, closing BLOCKER-02 with 12-row explicit calendar-to-archetype lookup plus Partner-to-Farva rename.

## What Was Built

`library/macro-templates.md` now opens with a `## Session-Type Mapping` section containing:
- A 12-row table mapping every distinct `Session` column value from `calendar/cycling-2026.md` to a `session_type` token, an archetype token (`lower_carb` / `moderate_carb` / `high_carb`), and a named macro template heading
- Archetype token legend explaining the semantic meaning of each archetype
- 4-step resolution procedure for commands executing the CAL-02 "find macro archetype by session_type" step

The existing template bodies are preserved exactly. Template labels promoted from flat text (`Rest Day Template:`) to H3 headings (`### Rest Day Template`) under a `## Jonas` section. Each Jonas template has an `**archetype:**` tag. The `Partner` section header is now `## Farva`.

## Tasks Completed

| Task | Description | Commit |
|------|-------------|--------|
| 1 | Add Session-Type Mapping table + H2/H3 headings + Partner→Farva | 1d95926 |

## Deviations from Plan

None — plan executed exactly as written. The plan specified H3 for sub-template headings and the structure matches the plan's target shape.

## Verification Results

| Check | Result |
|-------|--------|
| `## Session-Type Mapping` heading present | PASS (line 10) |
| 12 session_type token rows in table | PASS |
| `Partner` occurrences | PASS (zero) |
| `Farva` occurrences | PASS (2: resolution procedure + section header) |
| All 4 Jonas template headings present | PASS (lines 44, 53, 62, 71) |
| Macro number ranges unchanged | PASS (verified against original) |

## Known Stubs

None. The mapping table is complete and references real template headings in the same file. All 12 session values from the calendar have mapped rows.

## Threat Flags

None. This is a markdown-only library edit with no new network surface, auth paths, or schema changes.

## Self-Check: PASSED

- `library/macro-templates.md` exists and contains all required content
- Commit `1d95926` exists in git log
