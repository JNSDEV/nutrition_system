---
phase: 04.1-library-wiring-fixes
plan: 04
type: execute
wave: 2
depends_on:
  - "04.1-01"
files_modified:
  - library/calorie-targets.md
  - library/portions.md
  - library/README.md
  - templates/meal-prep-planner.md
autonomous: true
requirements:
  - LIB-04
  - CAL-02
  - TRK-02
  - CMD-01
  - CMD-02
  - CMD-05
  - CMD-06

must_haves:
  truths:
    - "grep -rn \"\\bPartner\\b\" library/ templates/ returns zero matches across the four target files"
    - "library/calorie-targets.md uses Farva as the section label"
    - "library/portions.md uses Farva as the section label"
    - "library/README.md uses Farva in all active description lines"
    - "templates/meal-prep-planner.md uses Farva in container label line"
  artifacts:
    - path: "library/calorie-targets.md"
      provides: "Farva branch label matching command prose"
      contains: "Farva:"
    - path: "library/portions.md"
      provides: "Farva portion label matching command prose"
      contains: "Farva:"
    - path: "library/README.md"
      provides: "Correct Farva display name in index descriptions"
      contains: "Farva"
    - path: "templates/meal-prep-planner.md"
      provides: "Farva label in container checklist"
      contains: "Farva"
  key_links:
    - from: "commands prose (\"Farva branch\")"
      to: "library/calorie-targets.md section label"
      via: "label match at runtime"
      pattern: "Farva:"
    - from: "commands prose (\"Farva's portions\")"
      to: "library/portions.md section label"
      via: "label match at runtime"
      pattern: "Farva:"
---

<objective>
Sweep `Partner` → `Farva` (and `partner/` → `farva/` if present) across the four operational files that the Phase 4 terminology pass missed: `library/calorie-targets.md`, `library/portions.md`, `library/README.md`, and `templates/meal-prep-planner.md`.

Purpose: WARNING-01 / WARNING-05 — commands instruct the model to read the "Farva branch" of library files, but those files still use "Partner" as the section label. The mismatch causes unreliable resolution on every Farva-scoped command run.

Note: `library/macro-templates.md` and `library/recipes.md` are handled by Plans 01 and 03 respectively. This plan covers the remaining four files.

Output: All four files have "Farva" where "Partner" previously appeared. Post-edit grep returns zero `\bPartner\b` matches across `library/` and `templates/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.1-library-wiring-fixes/04.1-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Current Partner occurrences to replace (from file reads above): -->

<!-- library/calorie-targets.md line 17: "Partner:" → "Farva:" -->
<!-- library/portions.md line 18: "Partner:" → "Farva:" -->
<!-- library/README.md lines 18, 21, 41: "Partner" in description text → "Farva" -->
<!--   line 18: "goals.md — Weight, performance, and behaviour targets for Jonas and Partner" -->
<!--   line 21: "recipes.md — Per-meal ingredient quantities split by Jonas vs Partner portions" -->
<!--   line 41: "trackers/ = per-person logs (Phase 2 — Jonas and Partner each get a `daily/`, `weekly/`, and `progress.md`)" -->
<!-- templates/meal-prep-planner.md line 19: "Label containers: Jonas / Partner / Lunch" → "Jonas / Farva / Lunch" -->

<!-- D-11: Do NOT edit .planning/phases/0[1-4]-*/ files (frozen historical artifacts) -->
<!-- D-10: Replace Partner (display) and partner/ (path) with Farva / farva/ in operational files only -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Partner → Farva in four remaining operational files</name>
  <files>library/calorie-targets.md, library/portions.md, library/README.md, templates/meal-prep-planner.md</files>
  <action>
Make targeted string replacements in each file. No structural changes — only the Partner → Farva rename.

**library/calorie-targets.md:**
- Line 17: `Partner:` → `Farva:`

**library/portions.md:**
- Line 18: `Partner:` → `Farva:`

**library/README.md:**
- Line 18: `...targets for Jonas and Partner` → `...targets for Jonas and Farva`
- Line 21: `...split by Jonas vs Partner portions` → `...split by Jonas vs Farva portions`
- Line 41: `Jonas and Partner each get a` → `Jonas and Farva each get a`

**templates/meal-prep-planner.md:**
- Line 19: `Label containers: Jonas / Partner / Lunch` → `Label containers: Jonas / Farva / Lunch`

After making all edits, run the verification grep to confirm zero remaining matches.
  </action>
  <verify>
    <automated>grep -rn "\bPartner\b" /Users/jonasockerman/Documents/nutrition_system/library/calorie-targets.md /Users/jonasockerman/Documents/nutrition_system/library/portions.md /Users/jonasockerman/Documents/nutrition_system/library/README.md /Users/jonasockerman/Documents/nutrition_system/templates/meal-prep-planner.md || echo "PASS: zero Partner occurrences in target files"</automated>
  </verify>
  <done>
    - `grep -rn "\bPartner\b" library/calorie-targets.md library/portions.md library/README.md templates/meal-prep-planner.md` returns no output (zero matches)
    - `grep "Farva:" library/calorie-targets.md` returns the section label line
    - `grep "Farva:" library/portions.md` returns the section label line
    - `grep "Farva" templates/meal-prep-planner.md` returns the container label line
  </done>
</task>

<task type="auto">
  <name>Task 2: Full-scope post-edit Partner grep across library/ and templates/</name>
  <files></files>
  <action>
Run a comprehensive grep across all files in `library/` and `templates/` to confirm zero `\bPartner\b` occurrences remain. This covers the four files edited in Task 1 plus any that may have been missed.

Command to run:
```bash
grep -rn "\bPartner\b" library/ templates/
```

If any matches are returned from files NOT in `.planning/phases/0[1-4]-*/`, edit those files to complete the sweep. If matches appear only in `.planning/phases/0[1-4]-*/` paths, they are frozen artifacts — do NOT edit them (per D-11).

Report the grep output in the summary.
  </action>
  <verify>
    <automated>grep -rn "\bPartner\b" /Users/jonasockerman/Documents/nutrition_system/library/ /Users/jonasockerman/Documents/nutrition_system/templates/ || echo "PASS: zero Partner occurrences across library/ and templates/"</automated>
  </verify>
  <done>
    - `grep -rn "\bPartner\b" library/ templates/` returns empty output (zero matches)
    - Summary records the pre-edit baseline count and the post-edit count of 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human → library files | Developer or Claude editor renames display labels |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-06 | Tampering | library operational files | accept | Markdown-only system. Grep verification confirms complete sweep. Frozen .planning/phases/ files excluded by D-11. |
</threat_model>

<verification>
After both tasks complete:
1. `grep -rn "\bPartner\b" library/ templates/` returns empty
2. `grep "Farva:" library/calorie-targets.md` returns match
3. `grep "Farva:" library/portions.md` returns match
4. `grep "Jonas / Farva / Lunch" templates/meal-prep-planner.md` returns match
</verification>

<success_criteria>
Zero `\bPartner\b` occurrences across `library/` and `templates/`. Every runtime file that commands read for "Farva branch" data now uses "Farva" as the section label, matching command prose exactly.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-library-wiring-fixes/04.1-04-SUMMARY.md` following the summary template. Include the pre-edit baseline grep count and post-edit zero count.
</output>
