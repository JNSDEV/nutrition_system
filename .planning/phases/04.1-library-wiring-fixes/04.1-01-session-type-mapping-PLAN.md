---
phase: 04.1-library-wiring-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - library/macro-templates.md
autonomous: true
requirements:
  - CAL-02
  - CMD-01
  - CMD-02
  - CMD-03
  - CMD-05
  - CMD-06

must_haves:
  truths:
    - "library/macro-templates.md has a Session-Type Mapping table at the top of the file"
    - "Every distinct Session value from calendar/cycling-2026.md has a row in the mapping table"
    - "Each mapping row resolves to a session_type token, an archetype token, and a macro template heading"
    - "Existing template bodies (Rest Day, 60 min Z2, SST/Tempo, Long Z2, Standard Cut Day, Lighter Day) are preserved unchanged"
    - "Partner label is renamed Farva in the Jonas/Farva section headers (per D-10)"
  artifacts:
    - path: "library/macro-templates.md"
      provides: "Session-type mapping table + preserved macro template bodies"
      contains: "## Session-Type Mapping"
  key_links:
    - from: "calendar/cycling-2026.md Session column"
      to: "library/macro-templates.md mapping table"
      via: "session_type token lookup"
      pattern: "Session-Type Mapping"
    - from: "library/macro-templates.md mapping table"
      to: "macro template headings in same file"
      via: "archetype token + template heading column"
      pattern: "archetype.*template"
---

<objective>
Add a `## Session-Type Mapping` table to `library/macro-templates.md` that explicitly maps every distinct Session column value from `calendar/cycling-2026.md` to a `session_type` token, an archetype token, and a named macro template heading.

Purpose: BLOCKER-02 — the CAL-02 contract states that `macro-templates.md` is authoritative for session_type → archetype resolution, but the file contains no such mapping. All 5 non-shopping commands fail at the "find macro archetype by session_type" step. This plan closes that gap.

Output: Updated `library/macro-templates.md` with the mapping table prepended before existing template bodies. Also fixes the stale `Partner` label to `Farva` per D-10 (opportunistic — same file, same commit).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04.1-library-wiring-fixes/04.1-CONTEXT.md
@.planning/v1.0-MILESTONE-AUDIT.md

<interfaces>
<!-- Current macro-templates.md structure — executor edits this file -->
<!-- Existing template headings that the mapping table must reference: -->
<!--   Jonas: "Rest Day Template", "60 min Z2 Template", "SST / Tempo Template", "Long Z2 Template" -->
<!--   Farva: "Standard Cut Day", "Lighter Day" -->

<!-- calendar/cycling-2026.md — Session column values that MUST appear in the mapping table: -->
<!--   Mon: "Z1 commute (both legs)" -->
<!--   Tue: "Intensity (VO2 / Threshold, alt.)" -->
<!--   Wed: "Z2 Zwift indoor" -->
<!--   Thu: "OFF" -->
<!--   Fri: "Fasted Z2 (75 min) + Z1 commute home" -->
<!--   Sat: "Strength (full body)" -->
<!--   Sun standard: "Long Z2 outdoor (variable)" -->
<!--   Sun progression: "105 km gravel SPORTIVE", "180 km road BENCHMARK", "140 km gravel REHEARSAL", "HEATHLAND 161 km gravel (event)" -->
<!--   Sun recovery rows: various km/distance descriptions (map these as long_ride) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prepend Session-Type Mapping table to library/macro-templates.md and rename Partner → Farva</name>
  <files>library/macro-templates.md</files>
  <action>
Edit `library/macro-templates.md` as follows:

**Step 1 — Add `## Session-Type Mapping` section at the top** (immediately after the YAML frontmatter block and the `# Macro Templates` H1, before any existing template content).

The table covers every distinct Session value from `calendar/cycling-2026.md`. Use these exact rows:

```markdown
## Session-Type Mapping

Maps each calendar Session label to a `session_type` token, an archetype token, and the macro template to apply.

| Calendar Session label | session_type | archetype | Macro template |
|------------------------|--------------|-----------|----------------|
| `OFF` | `rest` | `lower_carb` | Rest Day Template |
| `Z1 commute (both legs)` | `easy_ride` | `lower_carb` | Rest Day Template |
| `Z2 Zwift indoor` | `medium_ride` | `moderate_carb` | 60 min Z2 Template |
| `Fasted Z2 (75 min) + Z1 commute home` | `medium_ride` | `moderate_carb` | 60 min Z2 Template |
| `Strength (full body)` | `strength` | `lower_carb` | Rest Day Template |
| `Intensity (VO2 / Threshold, alt.)` | `intensity` | `high_carb` | SST / Tempo Template |
| `Long Z2 outdoor (variable)` | `long_ride` | `high_carb` | Long Z2 Template |
| Any Sunday long ride (km distances, recovery or progression) | `long_ride` | `high_carb` | Long Z2 Template |
| Any row with `SPORTIVE` marker | `long_ride+SPORTIVE` | `high_carb` | Long Z2 Template |
| Any row with `BENCHMARK` marker | `long_ride+BENCHMARK` | `high_carb` | Long Z2 Template |
| Any row with `REHEARSAL` marker | `long_ride+REHEARSAL` | `high_carb` | Long Z2 Template |
| Any row with `HEATHLAND` marker | `long_ride+HEATHLAND` | `high_carb` | Long Z2 Template |

**Archetype tokens:**
- `lower_carb` — protein-forward, moderate kcal, lower carbohydrate
- `moderate_carb` — balanced, mid-range kcal
- `high_carb` — carb-heavy, elevated kcal, fueling for significant training load

**Resolution procedure for commands:**
1. Read today's row from `calendar/cycling-2026.md` (`Session` column).
2. Find the matching row in the table above.
3. Use the `archetype` token to select the macro template heading below.
4. For Jonas: apply that template's ranges; for Farva: always use Standard Cut Day or Lighter Day regardless of Jonas's session_type.
```

**Step 2 — Add H2 headings to the existing Jonas templates.** The existing flat `Rest Day Template:`, `60 min Z2 Template:`, `SST / Tempo Template:`, `Long Z2 Template:` labels are plain text. Convert them to `## Rest Day Template`, `## 60 min Z2 Template`, `## SST / Tempo Template`, `## Long Z2 Template` under a `## Jonas` H2. Add an `**archetype:** lower_carb` / `moderate_carb` / `high_carb` tag under each template heading matching the mapping table above.

**Step 3 — Rename `Partner` → `Farva`** (per D-10). The existing `Partner` section header becomes `## Farva`. Apply same H2 treatment to `Standard Cut Day:` → `## Standard Cut Day` and `Lighter Day:` → `## Lighter Day` under `## Farva`.

**What NOT to change:** macro number ranges (`Calories:`, `Protein:`, `Carbs:`, `Fat:` lines) — preserve exactly as-is.

Final file structure:
```
---
(frontmatter)
---
# Macro Templates
## Session-Type Mapping
(table + procedure)
## Jonas
### Rest Day Template
**archetype:** lower_carb
(macro lines)
### 60 min Z2 Template
**archetype:** moderate_carb
...
### SST / Tempo Template
**archetype:** high_carb
...
### Long Z2 Template
**archetype:** high_carb
...
## Farva
### Standard Cut Day
(macro lines)
### Lighter Day
(macro lines)
```
  </action>
  <verify>
    <automated>grep -n "Session-Type Mapping" /Users/jonasockerman/Documents/nutrition_system/library/macro-templates.md && grep -c "archetype" /Users/jonasockerman/Documents/nutrition_system/library/macro-templates.md && grep -n "Farva" /Users/jonasockerman/Documents/nutrition_system/library/macro-templates.md && grep -v "Partner" /Users/jonasockerman/Documents/nutrition_system/library/macro-templates.md | grep -c "Partner" || echo "PASS: no Partner occurrences"</automated>
  </verify>
  <done>
    - `library/macro-templates.md` opens with `## Session-Type Mapping` table immediately after the H1
    - Table has rows for every Session value in `calendar/cycling-2026.md` (OFF, Z1 commute, Z2 Zwift indoor, Fasted Z2, Strength, Intensity, Long Z2, SPORTIVE/BENCHMARK/REHEARSAL/HEATHLAND markers)
    - Each row has session_type, archetype, and macro template heading
    - Existing macro number ranges are unchanged
    - "Partner" does not appear anywhere in the file; "Farva" is used instead
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human → library file | Human or Claude editor writes library/macro-templates.md |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04.1-01 | Tampering | macro-templates.md | accept | Markdown-only system; no auth layer. File is edited by the developer directly. |
| T-04.1-02 | Repudiation | session_type mapping | accept | Git history tracks all edits; no runtime execution risk in markdown. |
</threat_model>

<verification>
After task completes:
1. `grep "## Session-Type Mapping" library/macro-templates.md` — must return a match
2. `grep -c "session_type" library/macro-templates.md` — must return >= 10 (one per mapping row)
3. `grep "Partner" library/macro-templates.md` — must return empty (no matches)
4. `grep "Farva" library/macro-templates.md` — must return at least one match
5. `grep "Rest Day Template\|60 min Z2\|SST / Tempo\|Long Z2" library/macro-templates.md` — all four template headings still present
</verification>

<success_criteria>
`library/macro-templates.md` contains an explicit Session-Type Mapping table at the top with one row per distinct calendar Session label. Commands executing the CAL-02 "find macro archetype by session_type" step will find the mapping without inference. The file has no "Partner" occurrences.
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-library-wiring-fixes/04.1-01-SUMMARY.md` following the summary template.
</output>
